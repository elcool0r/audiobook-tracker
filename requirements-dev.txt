# Test-only dependencies. Deliberately NOT installed into the production image:
# shipping mongomock alongside the app is what made the silent in-memory database
# fallback reachable in production (see tracker/db.py).
-r requirements.txt
mongomock==4.1.2
pytest==7.4.3
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
httpx==0.24.0
